---
description: Build or run the diligence Q&A pack
argument-hint: "[anticipate | answer <question ref>]"
---

Load the `debretts-qa-pack` skill.

With `anticipate`, build the sixty-question pack from the buyer-flag list and the IM. With `answer`, draft from the data room, cite the index reference, and check consistency against every answer already given before returning it. Nothing is sent.
